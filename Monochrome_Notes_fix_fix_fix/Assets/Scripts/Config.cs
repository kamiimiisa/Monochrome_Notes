namespace Config {
    public enum Line {
        Line1,
        Line2,
        Line3,
        Line4
    }

    public enum NoteType {
        Touch,
        HoldStart,
        Hold,
        HoldEnd,
    }

    public enum Judge {
        Pafect,
        Graet,
        Miss,
        HoldStart,
        Hold,
        HoldEnd,
    }

    public enum Sound {
        BGM,
        SE
    }

    public enum SceneName {
        Main,
        MusicSelect,
        Title,
        Tutorial,
        Exit,
    }
}